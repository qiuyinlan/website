class AnyTests extends AbstractTestCase{"@test any should should invoke callback on each item as long as you return false"(){let e=this.newObject();let t=this.toArray(e);let s=[];let a;a=e.any(function(e){s.push(e);return false});this.assert.equal(a,false,"return value of obj.any");this.assert.deepEqual(s,t,"items passed during any() should match")}"@test any should stop invoking when you return true"(){let e=this.newObject();let t=this.toArray(e);let s=t.length-2;let a=s;let n=[];let r;r=e.any(function(e){n.push(e);return--s<=0});this.assert.equal(r,true,"return value of obj.any");this.assert.equal(n.length,a,"should invoke proper number of times");this.assert.deepEqual(n,t.slice(0,-2),"items passed during any() should match")}"@test any should return true if any object matches the callback"(){let e=emberA([0,1,2]);let t;t=e.any(e=>Boolean(e));this.assert.equal(t,true,"return value of obj.any")}"@test any should produce correct results even if the matching element is undefined"(e){let t=emberA([undefined]);let s;s=t.any(()=>true);e.equal(s,true,"return value of obj.any")}}runArrayTests("any",AnyTests);